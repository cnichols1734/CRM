# =============================================================================
# FLOOD HAZARD INFORMATION - DocuSeal Field Mapping
# =============================================================================
# This file maps CRM form fields to DocuSeal template fields.
# Generated by the Document Mapping UI
#
# Template ID: 2489522
# =============================================================================

template_id: 2489522
template_name: "Flood Hazard Information"
template_slug: "flood-hazard"

# Submitter roles for this template
submitter_roles:
  - Seller
  - Seller 2

# =============================================================================
# DIRECT FIELD MAPPINGS
# Format: crm_field: "DocuSeal Field Name"
# =============================================================================
field_mappings:

# =============================================================================
# COMPUTED/COMBINED FIELDS
# One DocuSeal field populated from multiple CRM fields
# =============================================================================
computed_fields:
  - docuseal_field: "Property address 1"
    template: "{property_address}, {property_city}, {property_state} {property_zip}"
  - docuseal_field: "Property address 2"
    template: "{property_address}, {property_city}, {property_state} {property_zip}"
  - docuseal_field: "Property address 3"
    template: "{property_address}, {property_city}, {property_state} {property_zip}"

# =============================================================================
# CONDITIONAL FIELDS
# Fields that are mapped based on another field's value
# =============================================================================
conditional_fields:
  {}

# =============================================================================
# OPTION TRANSFORMS
# Radio/checkbox values mapped to DocuSeal checkbox fields
# =============================================================================
option_transforms:
  {}

# =============================================================================
# VALUE TRANSFORMS
# =============================================================================
transforms:
  currency_fields:
    []
  date_fields:
    []
  radio_mappings:
    {}
  checkbox_to_x:
    []

# =============================================================================
# BROKER FORM FIELDS (fields that go to Broker submitter)
# =============================================================================
broker_form_fields: {}

# =============================================================================
# AGENT FIELDS (auto-populated from logged-in agent)
# =============================================================================
agent_fields: []
